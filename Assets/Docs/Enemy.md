# 敵実装のtodoリスト

各タスクはCompositeの思想を基に、制作される。

## 敵の攻撃処理

このクラスは、[通常状態][攻撃状態]の二つを持つ.
状態の流れをInspector上で編集可能.

### 通常状態

移動方向を向く.
一定時間後、状態を終了する.

### 攻撃状態

一定速度でプレイヤーの方向を向く.
一定時間毎に攻撃を放つ.
一定時間後、状態を終了する.

## ゲート

一定時間毎に敵を生成する.
敵は、オブジェクトプールを介して生成される.
一定のダメージを受けると消滅する.